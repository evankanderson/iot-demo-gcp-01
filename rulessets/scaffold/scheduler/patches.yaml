kinds:
  - serving.knative.dev/v1alpha1:Service
  - apps/v1:Deployment
labelsMatch:
  - airspot.krules.dev/type: ^rulesset$
patch:
  metadata:
    labels:
      app-domain: scheduler
  spec:
    template:
      spec:
        containers:
          - patch__0:
              volumeMounts:
                - name: config-krules-scheduler-mongodb-volume
                  mountPath: /krules/config/scaffold/scheduler/mongodb

        volumes:
          - name: config-krules-scheduler-mongodb-volume
            configMap:
              name: config-krules-scheduler-mongodb
